spring:
  jpa:
    hibernate:
      ddl-auto: validate
  application:
    name: user
  liquibase:
    change-log: classpath:/db/changelog/changelog-master.yaml

proxy:
  target-url: http://localhost:8080

jwt:
  expiration-time: 10m
  key:
    public: public-test.pem
    private: private-test.pem